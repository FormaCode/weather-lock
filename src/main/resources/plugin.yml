name: ${project.name}
main: ${project.mainClass}
version: ${project.version}

description: ${project.description}
author: ${project.author}
website: ${project.url}

commands:
  lockweather:
    aliases: [weatherlock]
    description: Allows to reload config, lock, and unlock weather in the sender's world